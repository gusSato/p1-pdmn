/**
 * Esse script serve apenas para fazer imports em outros scripts apenas usando o caminho
 * da pasta (o código irá importar por padrão o que estiver neste index.js, por
 * exemplo: 'src/components/LembreteLista')
 */
import LembreteLista from "./LembreteLista";

export default LembreteLista;